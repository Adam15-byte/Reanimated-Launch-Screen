import { StyleSheet, Text, View } from "react-native";
import React from "react";

const DropDownImages = ({ isThirdScreen }) => {
  return (
    <View>
      <Text>DropDownImages</Text>
    </View>
  );
};

export default DropDownImages;

const styles = StyleSheet.create({});
